meta {
  name: 204 No Content Insert
  seq: 5
}

script:pre-request {
  if (['qa', 'local'].includes(bru.getEnvVar("ENVIRONMENT"))) {
    const helpers = require('./_lib/helpers');
  
    const nino = helpers.randomNino();
    bru.setEnvVar("nino", nino);
  }
  
}
