meta {
  name: 400 Bad Request Insert
  seq: 7
}

script:pre-request {
  if (bru.getEnvVar("ENVIRONMENT") === 'local') {
    const helpers = require('./_lib/helpers');
  
    const nino = helpers.randomNino();
    bru.setEnvVar("nino", nino);

    const invalidRndNino = helpers.invalidNino;
    bru.setEnvVar("invalidNino", invalidRndNino);
  }
  
}
